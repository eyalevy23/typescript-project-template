{
  "name": "your-project-template",
  "version": "1.0.0",
  "description": "A template for a clean TypeScript project",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "dev": "concurrently \"tsc --watch\" \"nodemon dist/index.js\"",
    "start": "node dist/index.js",
    "test": "echo \"No tests specified\" && exit 1"
  },
  "keywords": [
    "typescript",
    "template",
    "starter"
  ],
  "author": "eyalevy",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9",
    "typescript": "^5.8.3"
  }
}
